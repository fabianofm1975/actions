name: DB Migrate
on: 
  push:
    branches: 
      - master
    paths:
      -  teste-db/migrations

jobs:
  teste:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "Step "

name: Run Migrations
on: 
  workflow_call:
    inputs:
      MIGRATION_COMMAND:
        description: The command to run on the migrate tool
        required: false
        type: string

      POSTGRES_DATABASE:
        description: Database where the migration will run
        required: false
        type: string
        
      POSTGRES_HOST:
        description: Database큦 address
        required: false
        type: string
     
      POSTGRES_PORT:
        description: Database큦 port
        required: false
        type: string

      POSTGRES_USER:
        description: Database큦 username
        required: false
        type: string

    secrets:
      POSTGRES_PASSWORD:
        description: Database큦 password
        required: false

jobs:
  migrate:
    runs-on: ubuntu-latest
    name: Run DB Migrations
    container: 
      image: migrate/migrate
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run Migrate
        run: migrate -verbose -path ${{ inputs.POSTGRES_DATABASE }}/migrations/changes -database postgres://${{ inputs.POSTGRES_USER }}:${{ secrets.POSTGRES_PASSWORD }}@${{ inputs.POSTGRES_HOST }}:${{ inputs.POSTGRES_PORT }}/${{ inputs.POSTGRES_DATABASE }} ${{ inputs.MIGRATION_COMMAND }}
